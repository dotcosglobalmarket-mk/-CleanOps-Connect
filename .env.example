PORT=4000
MONGO_URI=
MAPBOX_API_KEY=
JWT_SECRET=

# Stripe (Separate Charges and Transfers pattern — see docs/payments.md).
# No real keys yet; these are placeholders until a Stripe account is
# connected. Never commit real values here.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
